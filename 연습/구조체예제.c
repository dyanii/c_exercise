/*
#include <stdio.h>
#include <math.h>

struct  point {
	int x;
	int y;
};

int main(void)
{
	struct point p1;
	struct point p2;
	double xd;
	double yd;
	double dis;

	printf("점의 자표를 입력: ");
	scanf("%d %d", &p1.x, &p1.y);

	printf("점의 좌표를 입력: ");
	scanf("%d %d", &p2.x, &p2.y);

	xd = p1.x - p2.x;
	yd = p1.y - p2.y;
	dis = sqrt(xd * xd + yd * yd);
	printf("두 점 사이의 거리는 %f입니다.\n", dis);

	return 0;
}
*/