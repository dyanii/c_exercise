/*
#include <stdio.h>

struct rec {
	int x;
	int y;
};

int main(void)
{
	struct rec p1, p2;
	int xs, ys;
	int sq, dis;

	printf("왼쪽 상단 좌표 입력: ");
	scanf("%d %d", &p1.x, &p1.y);

	printf("오른쪽 하단의 좌료 입력: ");
	scanf("%d %d", &p2.x, &p2.y);

	xs = p2.x - p1.x;
	ys = p1.y - p2.y;
	sq = xs * ys;
	dis = 2 * xs + 2 * ys;

	printf("면적은 %d이고 둘레는 %d입니다.", sq, dis);

	return 0;
}
*/