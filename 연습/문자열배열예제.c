/*
#include <stdio.h>

int main(void)
{
	int i, n;
	char* fru[] = {
		"apple",
		"blueberry",
		"orange",
		"melon"
	};

	n = sizeof(fru) / sizeof(fru[0]);

	for (i = 0; i < n; i++)
		printf("%s \n", fru[i]);

	return 0;
}
*/