/*
#include <stdio.h>

struct account {
	int number;
	char name[20];
	int balance;
};

int main(void)
{
	struct account a1 = { 1,"ȫ�浿", 100000 };

	printf("{%d, %s, %d}", a1.number, a1.name, a1.balance);

	return 0;
}
*/